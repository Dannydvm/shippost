# ShipPost Configuration

# Server
PORT=3100

# Anthropic (for AI content generation)
ANTHROPIC_API_KEY=sk-ant-...

# Slack (for post previews/approvals)
SLACK_BOT_TOKEN=xoxb-...
SLACK_CHANNEL=social

# Post-Bridge (for multi-platform posting)
# Get API key from https://www.post-bridge.com/ ($5/month)
POST_BRIDGE_API_KEY=
POST_BRIDGE_API_URL=https://api.post-bridge.com

# Firebase (optional - for data persistence)
# If not set, uses in-memory storage (data lost on restart)
FIREBASE_CREDENTIALS={"type":"service_account",...}

# GitHub Webhook Secret (optional - for signature verification)
GITHUB_WEBHOOK_SECRET=
